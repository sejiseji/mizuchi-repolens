[project]
name = "mizuchi-repolens"
version = "0.1.0"
description = "Standalone, read-only repository insight viewer."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mizuchi = "mizuchi.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
